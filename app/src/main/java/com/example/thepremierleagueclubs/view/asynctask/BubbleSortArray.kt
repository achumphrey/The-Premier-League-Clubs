package com.example.thepremierleagueclubs.view.asynctask

import android.os.AsyncTask

/*
class BubbleSortArray : AsyncTask<IntArray, Void, Array<out IntArray>>() {

    override fun doInBackground(vararg arr: IntArray?): Array<out IntArray> {

            var n: Int = arr.size;
            var temp: IntArray? = null
            var i = 0
            var k = 1
            for (r in i until n){

                for (j in k until n){

   //                 if (arr[j-1] > arr[j]) {
                        //swap elements
                        temp = arr[j-1] as IntArray
                        arr[j-1] = arr[j] as Nothing
                        arr[j] = temp as Nothing
                    }
                }
            }
     //       return arr as Array<out IntArray>
    }

   //     var arr = intArrayOf(3, 60, 35, 2, 45, 320, 5)

        fun runCode(arr: IntArray){
            for (a in arr )
                print("$a, " )
        }



}

*/
